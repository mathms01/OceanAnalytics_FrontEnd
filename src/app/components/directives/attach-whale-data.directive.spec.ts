import { AttachWhaleDataDirective } from './attach-whale-data.directive';

describe('AttachWhaleDataDirective', () => {
  it('should create an instance', () => {
    const directive = new AttachWhaleDataDirective();
    expect(directive).toBeTruthy();
  });
});
